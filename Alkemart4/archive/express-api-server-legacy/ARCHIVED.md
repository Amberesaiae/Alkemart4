# ARCHIVED — Express API (`@workspace/api-server`)

**Frozen 2026-07-17.** Former path: `artifacts/api-server`.

Superseded by **`apps/backend`** (Mercur + Medusa, port 9000).

Reference only for port inventory / ETL ideas. Not a production write path.
