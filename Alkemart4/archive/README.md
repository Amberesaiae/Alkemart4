# Archive

Frozen code kept for archaeology only — **not** production write paths.

| Path | What |
|------|------|
| `workers-shell-vendor-sludge` | Mistaken generic green vendor SPA (wrong brand) |
| `workers-shell-admin-sludge` | Mistaken generic green admin SPA (wrong brand) |
| `alkemart-medusa-legacy` / `mercur-vendor-ui-legacy` / `express-*` | Pre–Workers stacks |

Canonical gold UIs live under `apps/storefront` and `apps/backend/apps/{ghana-vendor,admin}`.
