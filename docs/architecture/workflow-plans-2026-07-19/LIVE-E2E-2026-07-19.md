# Live e2e audit — 2026-07-19

Human-style flows against running API `:9000` + storefront `:5175`.  
No DB seed injection for the money path under test. Images from **Wikimedia Commons** (not Unsplash).

## Script

```bash
# Neon warm first (compute suspends)
echo 'select 1;' | neonctl psql medusa-prod \
  --project-id wispy-union-10280789 --database-name alkemart_marketplace

CURL_TIMEOUT=90 bash scripts/live-e2e-human-flows.sh
```

## Results (latest successful buyer path)

| Step | Result |
|------|--------|
| Catalog `min_price` (P2) | **OK** — e.g. Golden Palm 45 GHS |
| Peer offers `GET /store/alkemart/offers?product_id=` | **OK** |
| Static media | **OK** |
| Vendor login + `x-seller-id` | **OK** |
| Commons JPEG download (API → binary) | **OK** |
| Vendor multipart upload | **OK** → `/static/…-red-palm-oil.jpg` |
| Vendor product create (default variant) | **OK** `prod_01KXXFM6G6G9Y22TEA7DD9KWYW` |
| Vendor propose | Soft-gated (`invalid_data` quality/setup) — **admin publish works** |
| Vendor offer create | **OK** (needs warm Neon + ~60–120s) `offer_01KXXFY11X23A6DPC5JSX6T5SN` |
| Admin publish | **OK** as `admin@alkemart.local` |
| Buyer ATC `offer_id` | **OK** |
| Shipping options (seller-grouped map) | **OK** after flatten |
| Ghana COD checkout | **OK** `order_01KXXFTRB2HA7YJ8EDE0NC6S9R` |
| Guest order email lookup | **OK** |
| Wrong email denied | **OK** 404 |
| Store `seller_handle` filter | **OK** |

## Issues found → fixes

| Issue | Fix |
|-------|-----|
| Catalog `min_price` null | Catalog graph loads `prices.*`; GHS preferred |
| Peer offers global `limit=100` | `GET /store/alkemart/offers?product_id=` |
| Static 404s | Lab `packages/api/static/` placeholders + vendor uploads |
| Commons URL returned HTML | Resolve via Commons API + JPEG magic check |
| Product create rejects `prices`/`sales_channels` on variants | Mercur: prices on **offer**; simple default variant |
| Offer create needs inventory | `inventory_items` + `stock_levels` required |
| Shipping options not flat array | Mercur groups by seller — storefront already `flattenShippingOptions`; e2e fixed |
| Neon cold → vendor timeouts | `neonctl psql` wake; longer curl timeouts |
| Vendor propose soft gates | Admin publish is lab SoT; document quality gates |

## P1 / P2 plan status

- **P1 buyer integrity** — complete (cart transfer, checkout bind, order email policy, rate limits, seller catalog filter)
- **P2 catalog truth** — complete for prices + peer offers + location fields on search docs/facets (Meili when enabled)
- **Live human vendor + buyer** — complete (offer create after warm Neon)
