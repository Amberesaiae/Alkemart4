# SEO ops (SPA) — Phase 3 foundation

| Field | Value |
|-------|--------|
| **Date** | 2026-07-17 |
| **Status** | Partial — client meta + JSON-LD; no SSR yet |

## What is live

| Piece | Location |
|-------|----------|
| Title / description / OG / robots / canonical | `apps/storefront/src/lib/seo.ts` + `PageSeo` / `DocumentTitle` |
| Product JSON-LD + breadcrumbs | PDP `/product/$id` |
| Store JSON-LD | `/store/$slug` |
| Organization JSON-LD | Home `/` |
| `robots.txt` | `apps/storefront/public/robots.txt` |
| Static sitemap | `apps/storefront/public/sitemap.xml` |
| Facet / query noindex | Search multi-facet and `?q=` results |

## Env

```bash
# Production absolute URLs for canonical + OG
VITE_PUBLIC_SITE_URL=https://your-shop-domain
```

## Honest limits (SPA)

- Googlebot may not fully execute JS the same as SSR; **prerender/SSR for PDP** remains open.
- Static sitemap does **not** list every product — add a build job or API export later.
- Never invent ratings/reviews in schema; only API-backed fields.

## Next

1. Dynamic sitemap from published product handles (cron or build).  
2. Prerender PDP + category shells (Cloudflare / prerender.io / later SSR).  
3. Absolute URLs in `sitemap.xml` using production host.
